#include <numeric>
#include <unordered_map>
#include <vector>

namespace Statistics {
float find_average(std::vector<float> data);
float find_median(std::vector<float> data);
float find_mode(std::vector<float> data);
float find_range(std::vector<float> data);
} // namespace Statistics
